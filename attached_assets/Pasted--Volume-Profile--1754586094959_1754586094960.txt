📊 Объёмы слева (Volume Profile):
Отображаются в виде горизонтальных линий, каждая линия соответствует ценовому диапазону.

Высота линии = ширина ценового диапазона.

Цвет линии — составной:

🟩 Зелёная часть = процент объёма покупателей (buy).

🟥 Красная часть = процент объёма продавцов (sell).

Пример: 60% buy / 40% sell → линия на 60% зелёная, на 40% красная.

Цветовая пропорция отображается по ширине линии, визуализируя баланс.

При наведении — всплывающее окно: Buy: 12,000 | Sell: 8,000 | Delta: +4,000

Диапазон цен — максимально узкий, чтобы отображалось больше свечей.

Объёмы слева должны быть прикреплены к свечам и обновляться при скроллинге.

Визуально — как объёмы снизу, но горизонтальные, справа налево.

🕰️ Выбор периода:
В верхней части графика — шкала выбора периода, влияющая на отображаемые данные.

При изменении периода — обновляется график, объёмы и кластеры.

📦 Кластеры справа от свечей:
Ширина кластеров — немного увеличена.

Каждый кластер = объём в конкретном ценовом диапазоне внутри одной свечи.

Ширина кластера = объём.

Цвет кластера — составной:

Пропорция зелёного/красного = соотношение buy/sell в этом диапазоне.

Кластеры обновляются в реальном времени при поступлении новых данных.

Последняя свеча — кластер обновляется динамически.

Все данные сохраняются в базе + кэшируются.

📈 Свечи:
Ближе друг к другу, уже по ширине, визуально аккуратнее.

При скроллинге вверх/вниз:

Меняется временной диапазон.

Свечи увеличиваются/уменьшаются, добавляются/убираются.

Актуальная цена:

Всегда hovered.

Линия на графике с увеличенным weight.

Свечи не должны пересекать друг друга.

📉 Объёмы снизу:
Цветовая визуализация дельты bid/ask — смешанная пропорция:

Пример: 70% bid / 30% ask → 70% красная, 30% зелёная.

При наведении:

Отображается общая сумма объёма.

Разделение на bid/ask.

Время каждой свечи.

🧾 Стакан заявок:
Возможность изменять ценовой диапазон между уровнями.

Обновление в реальном времени, если есть новые данные.

На графике — линии крупных заявок.

Под блоком "Стакан заявок" — шкала, шириной ~80% от блока.

Ширину блока стакана — немного уменьшить.

То же самое — для блока объёмов слева.

📐 Шкала цен:
Сделать меньше, фон — background: none.

При скроллинге:

Вниз — диапазон цен увеличивается.

Вверх — диапазон цен уменьшается.

График и объёмы слева обновляются.

При наведении — курсор меняется, указывая на возможность изменения диапазона.

🎯 Общая цель:
Создать интерактивный график, сочетающий эстетику TradingView с кластерным анализом, реалтайм-обновлениями, визуализацией дельты объёмов, и гибким управлением масштабом и периодами.